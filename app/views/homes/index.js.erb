console.log('There are <%= @homes.size %> homes matched')

<% if @home.empty? %>
  $('.homes-list-group').html('<%= j(render 'empty_home') %>')
<% else %>
  $('.homes-list-group').html('<%= j(render @home) %>')
<% end %>
